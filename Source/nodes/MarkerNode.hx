package nodes;

import ash.core.Node;

import components.Marker;
import components.TilePosition;

// import components.Marker;

class MarkerNode extends Node<MarkerNode> {
    public var marker:Marker;
    public var position:TilePosition;
	
}