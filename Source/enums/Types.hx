package enums;

enum BuildingTypes {
    BASE;
}

enum Direction {
    RIGHT;
    UP;
    LEFT;
    DOWN;
}

enum EventTypes {
    LOOT;
    MINE;
    DISASTER;
    FATALITY;
}

enum SkillTypes {
    MINE;
    WALK;
    CARRY;
    ATTACK;
}

enum TileTypes {
    EMPTY;
    FLOOR;
    WALL;
    WALL_STONE;
    WALL_DAMAGED;
    BASE;
    WORKER;
    WORKER_ORE;
    ORE;
    PICKAXE;
}